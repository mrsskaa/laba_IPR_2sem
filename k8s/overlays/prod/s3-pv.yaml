apiVersion: v1
kind: PersistentVolume
metadata:
  name: s3-pv-messager-prod-uploads
spec:
  storageClassName: ""
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - allow-delete
    - region=us-east-1
    - url=http://minio.messager-prod.svc.cluster.local:9000
    - use_path_request_style
  csi:
    driver: ch.ctrox.csi.s3-driver
    volumeHandle: messager-prod-uploads-volume
    nodePublishSecretRef:
      name: csi-s3-secret
      namespace: messager-prod
    volumeAttributes:
      bucketName: messager-uploads-prod
      mounter: rclone
